var app = angular.module('cityPulse', ['ngAutocomplete']);

// $http.post('/api/tweets', {tweetParam: 'cityname'})
// .then((result) => console.log(result));

// window.fetch('/api/tweets', {tweetParam: 'cityname'});
